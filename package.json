{
  "name": "linkedin-query-loader",
  "version": "0.0.1",

  "engines": {
    "node": "7.4.0",
    "npm": "4.0.5"
  },
  "scripts": {
    "postinstall": "cd client && npm install",
    "rails-server": "echo 'visit http://localhost:3000/hello_world' && foreman start -f Procfile.dev",
    "test": "rspec"
  },
  "dependencies": {},
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ]
}
